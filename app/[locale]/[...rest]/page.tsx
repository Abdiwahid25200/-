import { notFound } from "next/navigation";

/**
 * 🐞 **الرابط الخاطئ كان يخرج من المتجر** — عطلٌ وجدتُه في الفحص
 *    (٠٤-٠٨)، وكانت قد صوّرته بنفسها قبله.
 *
 * `app/[locale]/not-found.tsx` كانت مكتوبةً منذ زمن **ولا تُستعمل
 * أبداً**: Next لا يعرضها إلا لمن نادى `notFound()` من داخل صفحةٍ
 * موجودة. أمّا المسار الذي لا يقابله ملفّ (`/pubgg` · رابطٌ قُطع عند
 * نسخه في واتساب) فلا يدخل تخطيط اللغة أصلاً — فيخرج بصفحة Next
 * الافتراضية: **إنجليزيةٌ بيضاء بلا ترويسة ولا قائمة ولا طريق رجوع**.
 *
 * وهذا المسار الجامع يلتقط ما لم يقابله ملفّ **داخل تخطيط اللغة**،
 * ثم ينادي `notFound()` — فتظهر صفحتنا بلغة الزائر وترويسة المتجر
 * وقائمته السفلية، ويعود بضغطة.
 *
 * ⚠️ **ولا يبتلع مساراً قائماً**: Next يقدّم المسار المحدّد على الجامع
 *    دائماً، فلا تمرّ به صفحةٌ لها ملفّها.
 * ⚠️ **والحالة تبقى ٤٠٤** (`notFound()` تكتبها): ولو أعدنا صفحةً
 *    عاديةً بحالة ٢٠٠ لفهرس جوجل ألف صفحةٍ فارغة.
 */
export default function CatchAll() {
  notFound();
}
