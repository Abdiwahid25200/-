# Ramaan Store — تعليمات المشروع

متجر إلكتروني عالمي (إنجليزي/صومالي) يُعاد بناؤه بـ Next.js. **تحدّث مع صاحب المشروع بالعربية دائماً.**
(العربية لغةُ الحديث معه — **وليست لغةً في الموقع**: حُذفت بقراره ٠٨-٠٨.)

## من هو

🚨 **صاحب المشروع رجل — يُخاطَب بصيغة المذكّر.** قالها بنفسه (٠٨-٠٨) بعد
أن خُوطب بالمؤنّث طويلاً. **والأرشيف في `docs/` مكتوبٌ بالمؤنّث خطأً —
لا يُقلَّد.**

غير مبرمج · يعمل من الآيباد والجوال فقط · لا يستطيع تشغيل الكود محلياً.
يراجع كل شيء عبر روابط معاينة Vercel. **أنت تكتب الكود، وهو يراجع ويقرّر.**

## قبل أي عمل

اقرأ `docs/الحالة.md` **فقط** — ٦٠ سطراً فيها الوضع الحالي والخطوة التالية والقرارات المعتمدة.

🔔 **وأوّل ما تقوله له في كل جلسة**: بنود قسم **«⏳ تنتظره»** في رأس
`docs/الحالة.md` — سطرٌ لكل بند، بلا شرحٍ إلا إن سأل. **طلبه هو**
(«ذكّرني في كل محادثة»): أشياءُ أجّلها بيده، وتذكيرُه بها مرّةً واحدة
في أوّل الجلسة يكفي — **ولا يُعاد في كل ردّ**، ولا يُنفَّذ منها شيء بلا
إذنه. وما أنجزه يُشطب من القسم في حينه.
`docs/خطة-Next.js.md` (٧٠٠٠ توكن) مرجع للتفاصيل — **لا يُقرأ في بداية الجلسة.**

**🔄 حدّث `docs/الحالة.md` في نهاية كل خطوة عمل** — هذا ما يُبقي الجلسة حيّة بعد الضغط.

## 🚨 تحذير استهلاك

`docs/الموقع-القديم/*.html` = **١٧٥ ألف توكن**. مرجع فقط — **لا تُقرأ كاملة أبداً.**
استخدم `Grep -n` ثم `sed -n 'X,Yp'` لاستخراج الكتلة المطلوبة فقط.

📖 التفاصيل الكاملة في السكِل: `ramaan-efficient-work`

## قواعد ثابتة

1. **لا تنفّذ قبل الشرح وأخذ الموافقة** — خطوة واحدة كل مرة
2. **ابنِ واختبر محلياً قبل كل رفع** (`npm run build`) — هي لا تستطيع الاختبار
3. **لا تحذف أو تستبدل ملفاً بدون سؤال**
4. عند وجود احتمالين — **اسأل، لا تخمّن**
5. عند الخطأ — **قف واشرح** ما صار ولماذا
6. **كل نص أو رقم يظهر للزبون يعيش في `lib/content.ts` أو `messages/`** — لا داخل المكوّنات، ليُدار من لوحة الإدارة لاحقاً
7. 🔒 **لا ترفع مفاتيح سرّية** — تُحفظ في Vercel Environment Variables، و`.env*` مستثناة في `.gitignore`

## 🔒 قرارات مقفلة — لا تُناقَش ولا تُبدَّل في أي جلسة

قالها مرّة، ولا يريد أن يقولها ثانية — لا لي ولا لجلسة أخرى:

| # | القرار |
|:--:|---|
| **أ** | **لوحة الإدارة بالإنجليزية** (`lang="en" dir="ltr"` في `app/admin/layout.tsx`). لا تُعرَّب — لا شلّها ولا شاشاتها ولا أزرارها. الموقع للزبون بلغتين، **واللوحة إنجليزية وحدها**. |
| **ب** | **أيقونات حقيقية فقط.** كل أيقونة مسارُ SVG في `components/icons.tsx`. **ممنوع**: الإيموجي (⚙ ✨ 📦)، والمحارف (`›` `‹` `＋` `↗` `$`)، والحروف بدل الرموز. المحرف يتغيّر شكله بين جهاز وجهاز، ولا يقبل حجماً ولا سماكة، ويظهر أحياناً مربّعاً فارغاً. |
| **ج** | **لكل شاشة أيقونتها هي** — لا أيقونة واحدة تتكرّر في ستّة أبواب. المكرّرة زينةٌ لا علامة. |
| **د** | **رابطٌ مباشر ومربّعُ نسخ — في كل مرّة.** انظر أسفله. |

### 🔒 (د) كل ما يُطلب منه: رابطٌ يفتح المكان · ومربّعٌ يُنسخ

قالها صراحةً (٠٣-٠٨): **«ما بدي أقول لك مرّة أخرى — في كل مرّة بدك منّي
أفعل شيئاً، بدي رابطاً مباشراً ونسخَ كل شيء أفعله».**

| القاعدة | الصواب | الخطأ |
|---|---|---|
| **الوصول** | رابطٌ يفتح **الصفحة المقصودة نفسها** | «افتح القائمة ← انزل ← اضغط التبويب» |
| **كل قيمة تُكتب** | مربّع كود، **قيمةٌ واحدة في كل مربّع** | قيمةٌ داخل جملة أو جدول تُقرأ ولا تُنسخ |
| **تعذّر الرابط** | يُقال صراحةً: «لا رابط لهذا، والطريق كذا» | صمتٌ ووصفُ طريق |

**ولماذا**: يعمل من الجوّال بيدٍ واحدة. كل كلمةٍ يكتبها بيده احتمالُ خطأٍ
يُكلّف ساعاتٍ من الانتظار (سجلّ نطاقٍ ناقصٌ حرفاً لا يُخبرك أنه ناقص).
والنقر على رابطٍ لا يُخطئ أحد.

**أمثلة روابط تُبنى ولا تُوصف**: لوحة Vercel ← `vercel.com/dashboard` ·
سجلّات Namecheap ← `ap.www.namecheap.com/Domains/DomainControlPanel/eramaan.com/advancedns`
· قواعد Firestore ← `console.firebase.google.com/project/ramaa-store/firestore/rules`
· Search Console ← `search.google.com/search-console/...?resource_id=sc-domain:eramaan.com`

**قبل أي رفع:** ابحث عن إيموجي أو محرف داخل JSX. وُجد؟ استبدله بأيقونة من
`icons.tsx`، أو ارسم واحدة جديدة هناك. (التعليقات العربية في الكود مسموحة —
المقصود ما **يظهر على الشاشة**.)

## تقني

| | |
|---|---|
| Next.js 16 (App Router) · TypeScript · Tailwind 4 · next-intl | |
| اللغات | `en` (افتراضية، بلا بادئة) · `so`. **لا عربية** — و`/ar/*` تُحوَّل إلى الإنجليزية (٣٠٨) في `next.config.ts` |
| الخط | **خطّ النظام** (كما بالمعاينة) · `.num` للأرقام |
| الألوان | **Coast** في `app/globals.css`. الأسماء القديمة محفوظة عمداً: `orange`=أخضر مائي · `yellow`=ذهب رملي · `navy`=العمق. النص فوق الأزرار = `text-onaccent` (ينقلب مع الوضع) |
| المعايير | لا خط < 12.5px · أهداف لمس ≥ 48px · حالة تركيز واضحة |
| Firebase | **مؤجّل للمرحلة ٣** — البيانات تجريبية داخل الملفات حالياً |

## سير العمل

`main` هو فرع الإنتاج — الرفع إليه ينشر على **`eramaan.com`** خلال دقيقة
(و`ramaan-store.vercel.app` يبقى يعمل. الإدارة محجوزة على `admin.eramaan.com`).
اختبر محلياً، ثم ارفع، ثم أعطه الرابط.

## أين يظهر كل قسم — قرار صاحب المشروع

| الصفحة | ما تعرضه |
|---|---|
| `/` الرئيسية | **الإلكترونيات وحدها** |
| `/games` | شدات ببجي · كوينز eFootball |
| `/accounts` | حسابات تيك توك · حسابات eFootball |
| `/help` | كل ما يخصّ الدعم |

الحقل `group` في `sections` هو ما يحدّد ذلك: `home` · `games` · `accounts`.

## التحكم بالأقسام

`lib/content.ts` ← مصفوفة `sections`. لكل قسم حالة:
`on` ظاهر ويعمل · `soon` ظاهر بشارة "قريباً" ولا يفتح · `off` مخفي تماماً.
تغيير الحالة يسري فوراً على صفحتَي الألعاب والحسابات وعلى القائمة الجانبية.
لإضافة قسم: أضيفي عنصراً بحالة `soon` — يظهر دون الحاجة لصفحة.

## التحقق من آيدي ببجي

`app/api/verify-pubg/route.ts` وسيط يخرج من السيرفر (يتجاوز CORS ويُخفي المفتاح).

| المتغيّر في Vercel | الدور |
|---|---|
| `PUBG_ID_API` | رابط الخدمة · `{id}` مكان الآيدي، وإلا أُضيف `?id=` تلقائياً |
| `PUBG_API_KEY` | 🔒 مفتاح RapidAPI — **لا يُرفع على GitHub أبداً** |
| `PUBG_API_HOST` | يُشتقّ من الرابط تلقائياً، فلا داعي لضبطه غالباً |

الوسيط يقرأ اسم اللاعب من أي شكل ردّ (`nickname`/`name`/`data.username`…)،
ويحفظ النتيجة ساعةً فلا يُنفَق نداء مدفوع مرّتين لنفس الآيدي،
ويحدّ الاستعمال بعشرين محاولة بالدقيقة لكل زائر فلا يُستنزف الرصيد.
بلا رابط يعمل الموقع بوضع التحقق اليدوي دون تعطيل الطلب.

## التحكم بالمنتجات والباقات

`lib/data.ts` ← لكل عنصر حقلان اختياريان:
`status`: `on` (افتراضي) · `soon` (معروض معطّل بوسم "قريباً") · `off` (مخفي).
`order`: رقم للترتيب، الأصغر أولاً. بلا رقم يبقى بترتيبه في المصفوفة.
مثال: `{ id: "u2", status: "soon", amount: 325, price: 6 }`.
الدوال `live()` و`isBuyable()` تُطبَّق في كل الصفحات والتدفّقات — فالتغيير يسري فوراً
على العدّادات وبطاقات المنتجات وبطاقات الباقات دون لمس أي صفحة.

## صفحة "طلباتي" وإشعار الطلبات

`components/OrderCard.tsx` — لكل طلب: أيقونة قسمه واسمه، ومسار ثلاثي
(قيد المراجعة ← مدفوع ← تم التسليم)، وسطر **"ماذا يحدث الآن؟" يخصّ القسم**
(`accountPage.next.<kind>` في `messages/`)، وزرّ واتساب يحمل رمز الطلب.

`app/api/notify-order/route.ts` — إشعار صاحب المتجر بكل طلب.
يُضبط بمتغيّر `ORDER_WEBHOOK` في Vercel، فيه `{msg}` مكان نصّ الرسالة.
الرسالة تكفي لتنفيذ الطلب بلا فتح الموقع، وفيها **رابط واتساب جاهز للزبون**
في أقسام الحسابات والإلكترونيات وحدها — في ببجي الرقمُ آيدي لعبة لا هاتف.
بلا المتغيّر لا يحدث شيء ولا ينكسر الطلب: الطلب في Firestore على كل حال.

## Firebase (المرحلة ٣)

`lib/firebase.ts` يقرأ ستّة متغيّرات `NEXT_PUBLIC_FIREBASE_*` من Vercel.
**بدونها لا ينكسر الموقع** — يعمل بالوضع اليدوي (ملخّص + واتساب) تماماً كالسابق.

| الملف | الدور |
|---|---|
| `lib/auth.tsx` | دخول بجوجل · `useAuth()` |
| `lib/orders.ts` | `saveOrder()` · `myOrders()` |
| `firestore.rules` | كل طلب وثيقة مستقلّة · الزبون يرى طلباته فقط · الأدمن وحده يعدّل |
| `storage.rules` | صور المنتجات عامة · الإيصالات خاصة |

🔒 الأدمن = وثيقة باسم الـuid داخل مجموعة `admins`، تُضاف من Firebase Console فقط.
الكتابة في `admins` ممنوعة على الجميع، فلا يستطيع أحد ترقية نفسه.

## تسجيل الدخول

`/login` بخطوتين — بالترتيب الذي طلبه صاحب المشروع:
① دخول بجوجل  ② إكمال التسجيل برقم الهاتف (جوجل لا يعطي الرقم، ونحتاجه للتسليم).
الرقم يُحفظ في `users/{uid}` — كل زبون يقرأ ويكتب وثيقته وحده.
`lib/profile.ts` فيه `dialCodes` (٢٢ دولة) و`isComplete()`.
`AuthProvider` فيه مهلة ٦ ثوانٍ: لو تعذّر الوصول لجوجل يُعتبر الزائر غير مسجّل
بدل شاشة انتظار أبدية.

## عرض الأقسام و"كيف يعمل المتجر"

`components/GameTile.tsx` — بلاطة مربّعة والاسم تحتها، ثلاث بالصف.
لكل قسم في `lib/content.ts`: `badge: "instant" | "manual"` و`img`.
**حقل `img` واحد يحكم ثلاثة مواضع**: بلاطة القسم · بند القائمة الجانبية ·
وأينما عُرض القسم. بلا صورة تظهر الأيقونة المرسومة فلا ينكسر شيء.
وفوق الصورة تدرّج داكن يضمن قراءة الاسم مهما كانت الصورة فاتحة.
`components/HowItWorks.tsx` بصفحة الدعم — `howItWorks.youtubeId` في `lib/content.ts`:
فارغ ⇒ يُخفى الفيديو وتبقى الخطوات الثلاث.

## لغة التصميم — Coast

| العنصر | القاعدة |
|---|---|
| الأرقام | صنف `.num` — خطّ ثابت العرض وأرقام متساوية، فتصطفّ كإيصال |
| بطاقة الباقة | **قسيمة ممزّقة**: `.voucher-notch` على كتلة التفاصيل + خط تقطيع منقّط |
| بلاطة القسم | أيقونة داخل **صفيحة سداسية** = وجه عملة |
| ترويسة الصفحة | `SectionHead` بوسم `eyebrow` **يخصّ كل صفحة** (مفاتيحه في `messages/*.json` ← `eyebrow`) |
| التوهّج | `.coast-glow` خلف الترويسة — بلا تجاوز أفقي |

⚠️ لا تضع `text-white` فوق `bg-orange` أو `bg-yellow` — استخدم `text-onaccent`.
⚠️ **ولا أدوات `rtl:` بعد اليوم** — لا لغةَ RTL في الموقع، فالصنف زينةٌ لا تعمل.

## البانر المتحرّك

`lib/content.ts` ← مصفوفة `slides`، ونصوصها في `messages/*.json` تحت `slides.<key>`.
لكل شريحة: `key` · `img` (اختيارية، داخل `public/images/slides/`) · `href`.
بلا صورة يظهر تدرّج بلون العلامة فلا ينكسر البانر.
يدور تلقائياً كل ٥ ثوانٍ، ويتوقّف عند اللمس أو التركيز، ويحترم "تقليل الحركة".
لإضافة شريحة: عنصر في `slides` + نصوصها بالترجمات — لا تعديل على أي صفحة.

## نظام الحركة والشعار

`app/globals.css` ← ثلاث أدوات تُستعمل بإضافة صنف واحد:
`.seq` على `<main>` ⇒ أقسام الصفحة تظهر متدرّجة · `.lift` على أي بطاقة ⇒
ترتفع عند المرور وتنكمش عند الضغط · `.nav-tap` لتبويبات القائمة السفلية.
كلها تُلغى تلقائياً لمن فعّل "تقليل الحركة" في جهازه.

`components/Logo.tsx` بألوان الهوية لا ثوابت: الحقيبة `--accent` والموجات
`--accent-2`، فيتبدّل مع الوضع الليلي وحده. أيقونة المتصفّح `app/icon.svg`.

## الهيدر

شعار مصمت (`<Logo solid />`) + الاسم + الوسم + **السلة** + زرّ القائمة.
السلة (`CartButton`) بجانب الهمبرغر بطلب صاحب المشروع — عدّاد المشتريات
مرئي في كل صفحة بلا فتح القائمة. الحساب يبقى داخل القائمة الجانبية.
وشعار المتجر يظهر في شاشة `/login` أيضاً بدل أيقونة عامّة.
**بلا خلفية ولا خطّ فاصل ولا التصاق بالأعلى** — يجلس على أرضية الصفحة ويمرّ معها.
(شفّاف + `sticky` = المحتوى يظهر من خلفه؛ التنقّل الدائم مكانه القائمة السفلية.)
