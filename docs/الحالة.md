# الحالة الآن — Ramaan Store

> ملف مختصر عمداً. يُقرأ في بداية كل جلسة بدل `خطة-Next.js.md` (٧٠٠٠ توكن).
> **يُحدَّث في نهاية كل خطوة عمل.** الخطة الكاملة مرجع فقط عند الحاجة لتفصيل.

**النطاق:** `eramaan.com` (Namecheap → Vercel)
**الإدارة:** `admin.eramaan.com` ⇐ يُوجَّه داخلياً إلى `/admin` من `proxy.ts`
(و`eramaan.com/admin` يبقى يعمل أيضاً). يلزم إضافة النطاق في Vercel و**CNAME**
باسم `admin` إلى `cname.vercel-dns.com` في Namecheap.
**آخر تحديث:** ٢٠٢٦-٠٨-٠١ · **الفرع:** `claude/extend-sessions-reduce-usage-9p90ip`

---

## ✅ منجز

| المرحلة | الحالة |
|---|---|
| ١ — بنية Next.js 16 + ثلاث لغات | ✅ |
| ٢ — كل الصفحات والتصميم | ✅ |
| زر العودة بكل صفحة داخلية | ✅ |
| أيقونات ملوّنة حقيقية | ✅ |
| بطاقات أقسام مضغوطة | ✅ |
| تحكّم بحالة الأقسام (`on`/`soon`/`off`) | ✅ |
| API التحقق من ببجي (`/api/verify-pubg`) | ✅ كود جاهز |
| تحكّم بالمنتجات والباقات (`status` + `order`) | ✅ |
| أيقونات SVG حقيقية لصف الضمانات | ✅ |
| ٣أ — أساس Firebase: دخول جوجل + حفظ الطلبات | ✅ |
| مشروع `ramaa-store` موصول · القواعد منشورة ومُختبَرة | ✅ |
| صفحة دخول بخطوتين: جوجل ← رقم الهاتف (`/login`) | ✅ |
| قائمة سفلية بزوايا دائرية · بلاطات أقسام ٣ بالصف · "كيف يعمل المتجر" | ✅ |
| **تصميم المعاينة مطبَّق حرفياً**: فاتح افتراضي · شبكة سداسيات · قسائم | ✅ |
| بانر متحرّك بثلاث شرائح · قائمة سفلية عائمة · صفحة دعم بصفوف | ✅ |
| هيدر شفّاف · إطار مدوّر · خطّ النظام · ترتيب الأقسام النهائي | ✅ |
| تسجيل الدخول يعمل على الآيباد (تمرير المصادقة على `eramaan.com`) | ✅ |
| صف الضمانات ووسائل الدفع: في `/help` وحدها | ✅ |
| **دردشة مباشرة** على Firebase — لا خدمة خارجية | ✅ كود جاهز |

⚠️ **الفوتر في صفحة الدعم وحدها** — قرارها. أُزيل من التخطيط العام
(`app/[locale]/layout.tsx`) وأُضيف في `app/[locale]/help/page.tsx`.
وأُعيد تصميمه بلغة Coast: خطّ تقطيع منقّط · صفيحة سداسية للشعار ·
السياسات أقراصاً · سطر حقوق بأرقام `.num`. ولا يكرّر ما فوقه في الصفحة
(التواصل ووسائل الدفع معروضة هناك أصلاً).

⚠️ **القائمة السفلية**: `bottom: 0.25rem` (كانت `0.6rem`). إنزالها يستلزم
إنزال ما يجلس فوقها بالمقدار نفسه — `--chat-bottom` في `globals.css`
و`FixedBar` — وإلا اتّسعت الفجوة أو تراكبا.

📐 **عرض الصفحة موحّد** — صنف `.page-w` في `globals.css` (٨٩٦ بكسل) على كل
الصفحات والهيدر وشريط التواصل والقائمة السفلية، فلا يقفز المحتوى عند التنقّل
على الآيباد. **لتغيير عرض الموقع كلّه: `max-width` في `.page-w` وحده**
(ومعه `max-w-4xl` في `FixedBar.tsx` — لا يقبل `page-w` لأنه `fixed`).
تبقى `/login` ضيّقة عمداً (٤٤٨) لأنها استمارة، و`/admin` سطح مستقلّ.

⚠️ **عرض الإطار على الآيباد** — `.app-shell` في `globals.css` **لا يُحذف منه
`width: 100%`**. `body` عنصر flex عمودي، والهامش `auto` الجانبي في flexbox
يُلغي التمدّد فيتقلّص الإطار إلى عرض محتواه — وبهذا كانت صفحة الألعاب تظهر
عموداً ضيّقاً وسط شاشة الآيباد بينما القائمة السفلية بعرض الشاشة كاملاً.

**الصفحات:** الرئيسية · الألعاب · الحسابات · ببجي · eFootball · تيك توك · حسابات eFootball · السلة · المساعدة · السياسات · الحساب · قريباً

---

## 🛠️ لوحة الإدارة — `/admin`

`app/admin/` خارج `[locale]` (**بالإنجليزية دائماً — قرارٌ مقفول**، بلا هيدر ولا دردشة، `noindex`).
`components/admin/AdminGate.tsx` يفحص `admins/{uid}` — **فحص واجهة فقط**،
والحماية الحقيقية في القواعد فلا ينفع تجاوزه.

`lib/overrides.ts` = **طبقة تعلو الملفات الثابتة ولا تمحوها**: ما يُحفظ في
Firestore يعلو `lib/content.ts` حقلاً بحقل. فرغت القاعدة أو تعذّر الوصول؟
يظهر الموقع كما هو اليوم — لا صفحة بيضاء. مهلة القراءة ٢٫٥ ثانية.

`lib/storage.ts` يرفع الصور عبر `/api/upload` إلى **Vercel Blob** ويحفظ
**رابطها** في Firestore (لا base64 — درسٌ من الموقع القديم).
الصفحات المعتمدة عليها `revalidate = 60`.

✅ مخزن Blob مربوط والرفع يعمل (٢٩-٠٧).
`lib/img.ts` يقرّر أيّ صورة تمرّ على `next/image`: المحلّية ومضيفاتنا
المعروفة تُحسَّن، وأي رابط ملصوق من مضيف آخر يُعرض بوسم `<img>` عادي —
فلا يظهر إطار مكسور في الهيرو أو البانر أو السلة.

**الحذف في اللوحة:** زرّ سلّة في صفّ الصنف مباشرة. الأصلي (من الملفات)
يُخفى ويظهر مشطوباً بكلمة `Removed` وبجانبه **Restore**، والمُضاف من
اللوحة يُمحى نهائياً. وحذف صورة صنفٍ أصلي صار يثبت (كان يعود الأصل).

## 🧾 صفحة "طلباتي" — لماذا كانت تظهر فارغة

الحفظ كان ناجحاً والقراءة هي التي تفشل: `myOrders` كان يجمع
`where(uid)` مع `orderBy(createdAt)`، وهذا يشترط **فهرساً مركّباً**
يُنشأ يدوياً من Firebase Console — وبدونه يرمي Firestore خطأً كان
يُبتلع بصمت فيظهر "لا طلبات بعد".

الحلّ: التصفية بالـuid وحدها (تكفيها الفهارس التلقائية) والترتيب في
المتصفّح. **قاعدة عامّة: لا `orderBy` مع `where` في هذا المشروع** — فلا
نحتاج إنشاء فهرس عند كل استعلام جديد.
وصار تعذّر القراءة يظهر رسالةً وزرّ "أعيدي المحاولة" بدل الصمت.

## 🛒 شريط الخطوات — السلة والألعاب

`components/FixedBar.tsx` يرسم الشريط في نهاية `<body>` عبر portal.
⚠️ **لا تجعله `fixed` داخل الصفحة**: حركة `.seq` تضع `transform` على أبناء
`<main>`، وأيّ أبٍ مُحوَّل يجعل `fixed` يقيس منه لا من الشاشة — وبهذا كان
شريط الألعاب يسقط أسفل الصفحة ولا يظهر أبداً.

ترتيب الخطوات في الألعاب (قرارها): **الآيدي ← تحقّق ← الباقة ← الدفع ← التأكيد**.
لا زرّ معطّل: الزرّ يقول الخطوة الناقصة ويأخذ الزبون إليها.
والتحقّق لازم في ببجي (`PubgFlow`)، ويُتجاوز إن تعذّرت الخدمة نفسها.

## 📋 شاشة الطلبات — لماذا أُعيدت

قالت: «عشوائية وصعبة». والسبب أن الصفّ كان يعرض **رمز الطلب** أوّلاً:
`M-537817` لا يقول شيئاً، و«660 UC ×1» يقول كل شيء.

| كان | صار |
|---|---|
| رمز الطلب في العنوان | **ما طُلب** في العنوان، والرمز سطرٌ صغير تحته |
| `$0.00` لطلبات النقاط | `120 pts` — الصفر لم يكن عطلاً بل دفعاً بالنقاط |
| `PENDING` / `DONE` | New · Paid · Delivered · Cancelled |
| تاريخٌ كامل في كل صفّ | **فاصل يومٍ** فوق المجموعة، والساعة وحدها في الصفّ |
| خمسة مرشّحات على سطرين | صفٌّ واحد يُمرَّر أفقياً |
| تفتح على كل الطلبات منذ الأزل | **تسأل أوّلاً**: أيّ يوم؟ ثم أيّها؟ |

⚠️ **الشاشة تسأل قبل أن تعرض** (`asking`): مدىً (اليوم · أمس · ٧ · ٣٠ ·
الكل · أو يومٌ من التقويم)، ثم أربع بطاقاتٍ ملوّنة بأعدادها —
**Waiting · Accepted · Delivered · Rejected**. والأعداد تتبع المدى
المختار فوقها، فتُقرأ الحصيلة قبل فتح القائمة.
وفي القائمة سطرٌ يقول ما اخترتِه وزرّ **Change** يعيدك للسؤال.

⚠️ **كل طرق الدفع العشر `status: "soon"` في `lib/data.ts:184`** — فخطوة
"اختاري طريقة الدفع" لا تظهر اليوم، وتظهر وحدها متى فُعّلت طريقة واحدة.
و`PaySection` لم يعد يختار الأولى تلقائياً — الاختيار قرار الزبون.

## 👤 صفحة الحساب — ما فيها وما ليس فيها

**فيها بطاقةٌ واحدة لا غير** (قرار صاحبة المتجر): من أنتِ · بريدك ·
رقمك · زرّ الخروج · ودعوة إكمال التسجيل لمن لم يُكملها.

⚠️ **لا طلبات ولا Barwaaqo ولا قائمة روابط.** لكلٍّ صفحته
(`/orders` · `/points` · `/cart` · `/help`) وكلّها في **القائمة
الجانبية**. وصفحةٌ تجمع كل شيء تُطيل التمرير ولا تُعجّل شيئاً.
ولذلك لم تعد `AccountPanel` تقرأ الطلبات أصلاً — قراءةٌ أقلّ في كل فتح
لا مجرّد إخفاء.

⚠️ وزرّ الخروج في **صفٍّ مستقلّ** لا بجانب الاسم: هناك يعوم زرّ الدردشة
فيغطّي طرفه، وبريدٌ طويل كان يضغطه حتى يكاد يختفي.

## 🧹 تدقيق الشكل والكسر (٠٢-٠٨)

فُحصت ١٦ صفحة آلياً: أخطاء الطرفية · التجاوز الأفقي · الخطوط الصغيرة ·
أهداف اللمس. ما وُجد وأُصلح:

| العلّة | الإصلاح |
|---|---|
| خطوط ٩٫٩ – ١١٫٥px في ١٥ ملفاً | `--text-xs: 0.79rem` مرّةً في `@theme` + استبدال كل `text-[0.6…]` |
| `.eyebrow` ١١٫٥px | ١٢٫٦px |
| نقاط البانر هدفُ لمسها ٨px | زرٌّ شفّاف ٤٤px والنقطة مرسومة في وسطه |
| فقاعة الدردشة ٣٧px | `min-h-11` |
| `window.location.origin` أثناء الرسم | يُقرأ بعد التركيب (`PointsCard` · `ReferralCard`) |
| السعر القديم ينزل سطراً في البطاقات الغالية | بلا `flex-wrap` والقديم `text-xs` يُقصّ |
| صنفٌ واحد في شبكة عمودين ⇒ نصف صفٍّ فارغ | عمودٌ واحد حين يكون الصنف واحداً |
| «✓» و«←» محارف تقوم مقام أيقونات | أيقونات مرسومة (القرار المقفول) |

⚠️ **باقٍ**: تحذير ترطيب React #418 يظهر متقطّعاً على صفحةٍ أو صفحتين
كل مرّة (ويتغيّر مكانه)، ولا يُعيد إنتاجه خادم التطوير. React يتعافى
منه ويعيد الرسم، ولا أثر له على الزبون — لم أُطارده أكثر.

## 🔎 محرّكات البحث والبحث الداخلي

`lib/seo.ts` — مصدر واحد للعناوين وروابط اللغات. `app/robots.ts` و
`app/sitemap.ts` تُبنيان من `lib/content.ts`، فالقسم المغلق يخرج وحده.
لكل صفحة قسم عنوانها ووصفها (`pageMeta`)، والسلة والحساب `noindex`.
وبطاقة `OnlineStore` (JSON-LD) في `layout` تقول لجوجل: هذا متجر.
⚠️ **`hreflang`** لازم لموقع بثلاث لغات، وبدونه يعدّها جوجل تكراراً.

`components/SearchButton.tsx` — بحث من الهيدر في كل ما يُشترى وفي
أسماء الأقسام. المصدر `mergedItems` نفسه، فالمضاف من اللوحة يُوجَد
فوراً والمخفيّ لا يظهر. يطبّع العربية (الألف والياء والتاء المربوطة).

⚠️ `meta.title` و`meta.description` **بالصومالية في اللغات الثلاث** —
نصّها هي، لم أغيّره. وهي أقوى إشارة في جوجل، فالعربية والإنجليزية
لا تظهران لباحث بهما. بانتظار قرارها.

## 🎁 نظام النقاط (المرحلة ٥) — الخطوة ١ منجزة

**النقطة = سنت** (`USD_PER_POINT` في `lib/points.ts`). ١٠٠ نقطة = دولار.

| الملف | الدور |
|---|---|
| `lib/points.ts` | القيمة · الإعدادات · حساب نقاط الطلب · قراءة الرصيد والسجلّ |
| `lib/adminOrders.ts` | قراءة كل الطلبات · تغيير الحالة **بمعاملة** تمنح النقاط أو تخصمها |
| `components/admin/OrdersEditor.tsx` | شاشة الطلبات — وفيها **رقم الزبون** وزرّ واتساب |
| `components/admin/PointsEditor.tsx` | تشغيل النظام · النقاط الافتراضية · الحدّ الأدنى للاستبدال |
| `components/PointsCard.tsx` | ما يراه الزبون في `/account`: رصيده وقيمته وسجلّه |

🔒 **الرصيد لا يُكتب من جهاز الزبون إطلاقاً** — `firestore.rules` تمنع تغيير
`points` في `users/{uid}`، والكتابة للأدمن وحده. ولولا ذلك لكتب أي زبون
لنفسه ما شاء من أدوات المتصفّح ثم اشترى به.

🧾 **`users/{uid}/ledger`** سجلّ كل حركة (كم · لماذا · لأي طلب). بدونه لا
نعرف كم نخصم عند الإلغاء. الزبون يقرأ سجلّه ولا يكتب فيه.

**متى تُمنح؟** عند «Mark paid» في اللوحة. **والإلغاء يخصم فوراً** (قرارها).
والرصيد لا ينزل تحت الصفر أبداً.

**نقاط كل باقة**: حقل `Points` في تبويب Products بجانب السعر. الفراغ ⇐
«النقاط الافتراضية» من تبويب Points.

✅ **الخطوة ٢**: خصم النقاط في السلة **وفي صفحات الألعاب** — `components/PointsRedeem.tsx`
(خطّاف واحد ومكوّن واحد يستعملهما `CartView` و`BuyFlow`، فلا شكلان لشيء واحد).
الخصم يُحفظ مع الطلب ويُخصم فعلاً عند تأكيد الدفع، والقواعد تشترط
ألّا يتجاوز رصيده الحقيقي المقروء من وثيقته.

✅ **شاشة الزبائن** — `lib/customers.ts` + `components/admin/CustomersEditor.tsx`.
كل الأرقام في مكان واحد ببحث بالاسم أو الرقم أو البريد، وزرّ واتساب،
وتعديل الرصيد يدوياً (هديّة أو تصحيح) — وكل تعديل يمرّ بسجلّ الحركات.
⚠️ بلا `orderBy` في الاستعلام: الوثيقة التي تنقصها `updatedAt` تسقط من
الترتيب فتختفي زبونة بلا سبب ظاهر. الترتيب في المتصفّح.

## 👥 المساعدون

`lib/staff.ts` + `components/admin/StaffEditor.tsx` + `lib/adminAccess.tsx`.

**الصلاحية بالبريد لا بالمعرّف (uid)** — المعرّف لا يُعرف إلا بعد أوّل
دخول، فلو بنينا عليه لَما استطاعت منح الصلاحية قبل أن يدخل المساعد.
والبريد يخدم الطريقتين: من يدخل بجوجل ومن يدخل باسم وكلمة سرّ.

ستة أبواب: `orders` · `customers` · `products` · `sections` · `points` · `faq`.
كل باب مربّعٌ في اللوحة، ومغلقُه لا يظهر تبويبه أصلاً.

📱 **رقم واتساب المساعد لازمٌ عند الإنشاء** (`phone` في `staff/{email}`):
إليه يُرسَل رمز التحقّق، وبه تصلين إليه حين يتعثّر. ويظهر في بطاقته
زرَّ واتساب جاهزاً، **ويُعدَّل من البطاقة نفسها** (زرّ Change / Add number)
بلا حذف المساعد وإنشائه من جديد.

🔒 **الحماية في القواعد لا في الواجهة**: `staffCan(p)` تُسأل قبل كل كتابة،
فمن عدّل الواجهة بأدوات المتصفّح رُفض عند الخادم. ومجموعة `staff`
**لا يكتبها إلا الأدمن** — ولو كتبها مساعد لمنح نفسه كل شيء.

⚠️ في `AdminGate` فحصُ `admins` في `try` مستقلّ عمداً: قراءتها مرفوضة
لغير الأدمن، ولو جُمع الفحصان لابتلع خطؤها فحصَ المساعد فلا يدخل أحد.

⚠️ تبويب Helpers يظهر لصاحبة المتجر وحدها (`role === "owner"`).

## 🚫 إلغاء الزبون طلبه

`lib/orders.ts` ← `cancelMyOrder()` و`canCancel()`. الزرّ في `OrderCard`
ما دامت الحالة ليست `done` ولا `cancelled`، **والسبب إلزاميّ** (٣ أحرف فأكثر).

القواعد (`validCustomerCancel`) تسمح بتغيير واحد: الحالة إلى `cancelled`
مع سبب، وترفض أن يمسّ الزبون مبلغاً أو أصنافاً أو نقاطاً، وترفض إلغاء
طلبٍ سُلّم أو أُلغي.

⚠️ **الزبون لا يملك رصيده**، فإلغاؤه لا يسوّي النقاط وحده: تظهر في
اللوحة لافتة حمراء وزرّ **Settle points** يعيد ما استُعمل ويسحب ما مُنح.
وإن كنتِ شحنتِ قبل إلغائه، اضغطي **Mark paid** فيكمل الطلب ولا تتكرّر
النقاط (`awarded !== 0` يمنع المنح مرّتين).

## 📊 التحليل والتكلفة والربح

`lib/costs.ts` + `components/admin/Analytics.tsx`.

🔒 **التكلفة في مجموعة `costs` المستقلّة، لا في `packages`** — تلك يقرأها
**كل زائر** (أسعار وصور)، فلو كانت التكلفة فيها لعرف أي شخص ربحك من كل
باقة بأدوات المتصفّح. القاعدة: `costs` **للأدمن وحده — ولا حتى المساعد**.

الزبون يرى **رقماً واحداً**: السعر. وأنتِ ترين التكلفة والربح والهامش.

⚠️ **الدخل من `paid` و`done` وحدهما** — وإلا صار كل طلبٍ وهميّ ربحاً على
الورق. و`total` هو المدفوع فعلاً (بعد خصم النقاط)، فالخصم محسوب في الربح.
وتظهر لافتة حين تُباع أصناف بلا تكلفة مكتوبة، لأن الربح حينها أعلى من الحقيقة.

## 🔒 مقفول — لا يُبدَّل في أي جلسة

**اللوحة بالإنجليزية** (`lang="en" dir="ltr"`) · **أيقونات SVG حقيقية لا
إيموجي ولا محارف** (`›` `⚙` `＋`) · **لكل شاشة أيقونتها هي**.
التفاصيل في `CLAUDE.md` ← «قرارات مقفلة». عُرِّبت اللوحة مرّةً فرُفضت، ورُدّت.

## 💳 لغة اللوحة — «العدّاد» (قرار صاحبة المتجر، بعد ثلاثة اتجاهات)

عُرضت ثلاثة اتجاهات (الطابور · العدّاد · الغُرَف) واختارت **العدّاد**:
ورقٌ فاتح `#f6f7fb`، حبرٌ أزرق `#123262`، أخضرُ ربح `#0f7a4d` — كتطبيق بنك.

⚠️ **الألوان تُعاد تعريفها على `.adm` في `globals.css` وحده**، فتتبعها كل
أدوات Tailwind داخل اللوحة (`bg-orange` تصير حبراً أزرق) بلا تعديل ستّ
عشرة شاشة. **ولا بدّ من تكرار أسماء `--color-*` هناك**: المُصرِّف يستبدل
`var(--accent)` داخل `--color-orange` بقيمتها عند `:root` وقتَ البناء،
فتغييرُ `--accent` وحده لا يصل إلى `bg-orange` — جُرّب فبقي الزرّ أخضر مائياً.

⚠️ واللوحة **فاتحة في الوضعين** (`data-theme="light"` + `color-scheme`):
أداةُ عملٍ تُقرأ في الشمس، ولوحةُ أرقامٍ تفقد نصف وضوحها على أرضٍ سوداء.

**التنقّل: أربعة تبويبات أسفل الشاشة** لا قائمة همبرغر:

| التبويب | ما فيه |
|---|---|
| الرئيسية | ربح اليوم كبيراً · أربعة مربّعات · آخر الطلبات |
| الطلبات | الطلبات · الدردشة · الزبائن |
| متجري | المنتجات · الأقسام · البانر · طرق الدفع |
| أرقامي | الأرباح · تقرير المساعدين · الدعوات |

⚠️ و**الإعدادات في ترس الترويسة** لا تبويباً خامساً: تُفتح مرّة كل شهر،
وتبويبٌ خامس يسرق مكاناً من أربعةٍ تُفتح كل يوم.

⚠️ `AdminMenu.tsx` لم يعد قائمةً — بقي فيه نوع التبويبات وأيقوناتها فقط،
وهو المصدر الذي يقرأ منه `AdminTabs`.

⚠️ **اللغة: الإنجليزية** — قرارٌ مقفول. عُرِّبت مرّةً فرُفضت ورُدّت كما كانت.

## 🍔 (سابقاً) تنقّل اللوحة بالهمبرغر

اثنا عشر تبويباً في صفٍّ واحد = شريطٌ يُمرَّر أفقياً لا يُرى نصفه.
صارت **أربع مجموعات مسمّاة بالعربية بالمهمّة**، مصدرها `GROUPS` في
`AdminTabs.tsx` وحده — والقائمة والرئيسية تُبنيان منه فلا يتفرّقان:

| المجموعة | سطرها | ما فيها |
|---|---|---|
| **كل يوم** | ما تفتحينه في كل جلسة | الطلبات · الدردشة · الزبائن |
| **متجري** | ما يراه الزبون ويشتريه | المنتجات · الأقسام · البانر · طرق الدفع |
| **أرقامي** | هل المتجر رابح؟ | الأرباح · تقرير المساعدين · الدعوات |
| **الإعدادات** | تُضبط مرّة ثم تُنسى | النقاط · بيانات المتجر · النصوص · الأسئلة · المساعدون · السلة |

⚠️ **لكل باب `note`: سطرٌ يقول ماذا يفعل لا ماذا يُسمّى.** «الطلبات»
وحدها اسمٌ يُخمَّن، و«اقبلي الطلب، أكّدي الدفع، سلّمي» تعليمة. والسطر
يظهر في ثلاثة مواضع: القائمة · بلاطة الرئيسية · تحت عنوان الشاشة المفتوحة.

⚠️ والرئيسية تفتح على **«ينتظرك الآن»**: طلبات لم تُؤكَّد · رسائل بلا
ردّ. سؤال من يفتح اللوحة «ماذا أفعل الآن؟» لا «أين قسم كذا؟».

⚠️ والمجموعة التي لا يملك المساعد شيئاً منها لا تظهر أصلاً.

⚠️ **الشلّ عربيّ، وداخلُ الشاشات إنجليزيّ بعد** (Products · Payments…)
— يُترجَم شاشةً شاشة عند الطلب.

## 🖼️ التحكّم بمحتوى الموقع من اللوحة

| التبويب | ما يتحكّم به |
|---|---|
| **Banner** | شرائح الرئيسية: صورة · ثلاثة نصوص بثلاث لغات · الرابط · الترتيب · إخفاء · إضافة وحذف |
| **Store info** | رقم واتساب · البريد · تيليجرام · ساعات العمل بثلاث لغات |
| **Sections** | صور الأقسام — وتظهر الآن في **القائمة الجانبية** أيضاً |
| **Payments** | كل طريقة دفع: الحالة · الاسمان · المشغّل · الأرقام · كود USSD · الترتيب · إضافة وحذف |
| **Page texts** | «كيف يعمل المتجر» بخطواته الثلاث ورمز الفيديو · السياسات الأربع — بثلاث لغات |
| **Store info** | **اسم المتجر ووسمه** · واتساب · البريد · تيليجرام · ساعات العمل · **زرّ إغلاق المتجر ودوام الطلبات** |

⚠️ **صفّ الدفع في `/help` من اللوحة أيضاً** — `PayChips` كان يقرأ
`lib/data.ts` وحده، فيقول «قريباً» عن طريقةٍ فعّلتها من اللوحة ولا يعرض
ما أضافته بيدها. صار يقرأ `mergedPay()` نفسه الذي تقرؤه السلة وصفحات
الألعاب، ومعه الشعار والاسم بلغة الصفحة.

⚠️ **طرق الدفع**: ما دامت كلّها `soon` لا تظهر خطوة الدفع أصلاً. اللوحة
تنبّه بلافتة حمراء حين لا توجد طريقة واحدة `on`. و`PaySection` يعرض
الأصل الثابت ريثما تصل قراءة اللوحة، فلا تقفز القائمة أمام الزبون.

⚠️ **`settings/how` و`settings/policy`**: مفتاحٌ مسطّح (`steps.choose.title`)
⇐ ثلاث لغات. الفارغ يعني «اتركي الأصل» من `messages/*.json`.

`mergedSite()` و`mergedSlides()` في `lib/overrides.ts` — الأصل الثابت
تعلوه الوثيقة، والفارغ يعني «اتركي الأصل».

⚠️ **`HeroSlider` يسأل `t.has` قبل كل ترجمة**: الشريحة المضافة لا مفتاح
لها في `messages/*.json`، ونداء `t` بمفتاح غائب يرمي خطأً فتسقط الرئيسية.

⚠️ `settings/store` لصاحبة المتجر وحدها · `slides` بباب `sections`.

## 🎮 إضافة الأقسام من اللوحة — تحكّم كامل

**المشكلة التي حُلَّت:** كانت الأقسام مصفوفةً ثابتة في `lib/content.ts`،
واللوحة تعدّلها ولا تضيف إليها. وإضافة لعبة كانت تعني مبرمجاً.

| القطعة | الدور |
|---|---|
| `SectionOverride.custom` | يميّز القسم المضاف عن الأصلي |
| `customSections()` · `mergedSections()` | تُلحق المضاف بالأصلي في كل الصفحات |
| **`app/[locale]/s/[key]/page.tsx`** | **صفحة تُولَد وحدها لكل قسم جديد** |
| `components/flows/CustomFlow.tsx` | تدفّق شراء عام: حقل واحد · `pay` أو `whatsapp` |
| `SectionsEditor` | إنشاء · حذف · المجموعة · شكل الشراء · الوسم · الترتيب |
| `ItemsEditor` | الأقسام المضافة تظهر في قائمة الأقسام فتُملأ بأصنافها |
| `MenuDrawer` | القسم الجديد يظهر في القائمة الجانبية |

⚠️ **`/s/{key}` ليست مبنيّة مسبقاً** (لا نعرف المفاتيح وقت النشر): تُبنى
عند أول طلب وتُخزَّن دقيقة. ولذلك القسم الجديد يظهر خلال دقيقة بلا نشر.

⚠️ **الحذف نهائي للمضاف وحده.** القسم الأصلي لا يُحذف — يُخفى بالحالة
`off`، لأن مسح تعديله لا يمحوه من `lib/content.ts` فيعود ويحتار من حذفه.

⚠️ `ItemKind` صار يقبل أي نصّ (`string & {}`): القسم المضاف أصنافه كلّها
في Firestore، و`fromStatic[kind]` يرجع فارغاً بدل أن ينهار.

## 🚪 صفحة الإغلاق — ثلاثة أشكال تُختار وتُكتب من اللوحة

`components/ClosedNotice.tsx` — ثلاثة أشكال في ملفٍ واحد، والاختيار
حقلُ `style` في `settings/store`:

| الشكل | ما هو |
|---|---|
| `curtain` **الستارة** | أرضٌ بلون العمق · الشعار · عدّادٌ تنازليّ · واتساب |
| `sign` **اللافتة** | لافتةٌ ورقية مائلة بحبلها وظلّها ووقتِ الفتح |
| `queue` **الطابور** | شريطٌ ملوّن + دعوةٌ للحجز — **ولا يمنع الطلب** |

⚠️ **`queue` وحده يُبقي البيع**: `canOrderNow()` تسمح بالطلب وهو مغلق،
و`isReservation()` تضع `reserved: true` على الطلب، فتراه اللوحة بوسم
**Reserved**. فلا تضيع طلبات الليل، ويُفتح الصباح على طابورٍ جاهز.

⚠️ **كل كلمةٍ تُكتب من اللوحة** بثلاث لغات: عنوانٌ وسطرٌ للإغلاق اليدويّ،
وآخران لانتهاء الدوام، وصورةٌ في الوسط (فارغةٌ ⇒ شعار المتجر). والفارغ
يعني «اتركي نصّ الترجمة» فلا تفرغ الشاشة أبداً.

⚠️ **العدّاد لا يُحسب على الخادم** — لو حُسب في الموضعين لاختلف الرقمان
بثوانٍ ووقع خطأ ترطيب في كل زيارة. يبدأ صفراً ويُملأ في المتصفّح.

## 🔒 إغلاق المتجر وساعات العمل

`lib/storeOpen.ts` + `components/ClosedNotice.tsx`. الإعدادات في
`settings/store` (نفس وثيقة بيانات المتجر): `closed` · `hoursOn` ·
`openFrom` · `openTo` · `offDays` · `tzOffset`. تُضبط من تبويب **Store info**.

⚠️ **الوقت بتوقيت المتجر لا بتوقيت جهاز الزبون** — `new Date(at + tzOffset*3600000)`
ثم `getUTC*`. زبونٌ في لندن ساعتُه غير ساعتها؛ لو حسبنا بجهازه لفُتح المتجر
وأُغلق في أوقات لا تعرفها. ودوامٌ يعبر منتصف الليل يُحسب على شقّين.

⚠️ **لا يُخفى المتجر**: التصفّح والأسعار تعمل، ويُمنع **الطلب** وحده —
`store.open` شرطٌ في `canConfirm` (BuyFlow) و`canOrder` (CartView)،
واللافتة تقول متى نفتح، والزرّ يقول السبب بدل أن يصمت تحت الضغط.

⚠️ **تعذّرت القراءة ⇒ مفتوح** (`DEFAULT_OPEN`) — عطلُ شبكةٍ لا يقفل متجراً.
والحالة تُراجَع كل دقيقة، والحالة الابتدائية «مفتوح» في الخادم والمتصفّح
معاً فلا يقع خطأ ترطيب.

## 📣 جذب الزبائن بلا تكلفة — ثلاثة أبواب

### ① شريط الثقة — `components/TrustBar.tsx` + `lib/stats.ts`

عدد الطلبات المسلَّمة · متوسّط دقائق التسليم · مفتوح الآن. في الرئيسية
تحت البانر.

⚠️ **الزائر لا يقرأ `orders`** (ولا يجوز)، فالعدّ في وثيقة مُجمَّعة
`settings/stats`: يقرأها الجميع ويكتبها `canDo('orders')`. وتُحدَّث
**خارج معاملة الحالة** — رقمُ عرضٍ لا يُفشل تسليم طلب. وتُعدّ عند
**الانتقال** إلى `done` لا عند كل حفظ.
⚠️ دون `MIN_ORDERS` (٢٠) يختفي العدّاد — رقمٌ ضعيف يهدم الثقة.
وزرّ **Recount from old orders** في تبويب Analysing يحسبها من الطلبات القديمة.

### ② العنوان والوصف — كانا بالصومالية في اللغات الثلاث

`meta.title` و`meta.description` صارا بلغة كل نسخة، وأُضيف `pageDesc`:
وصفٌ خاصّ بكل صفحة يقرأه `pageMeta` عبر `td.has(key)`. وصفٌ واحد مكرّر
يجعل الصفحات تتنافس بدل أن تتكامل.
⚠️ **الاختبار محلياً يستلزم `rm -rf .next`**: صفحات ISR تُخزَّن في
`.next/cache` بين البناءات فتُعرض بيانات قديمة. على Vercel لا يحدث ذلك.

### ③ الدعوة — `lib/referrals.ts`

رمزٌ لكل زبون (`RS` + ٥ حروف بلا `O/0/I/1`) في `refcodes/{code}` — `get`
وحده كدليل الأرقام: من يعرف الرمز يستعمله ولا يُسرد أحدٌ القائمة.

💡 **الجائزة بعد أوّل طلبٍ مدفوعٍ بمال** لا عند التسجيل. من سجّل ولم
يشترِ = صفر. ومن اشترى مُوِّلت جائزته من ربح بيعةٍ ما كانت لتوجد.

🔒 والمنح **في جلستك لحظة Mark paid** — داخل معاملة `setOrderStatus`،
فالزبون لا يملك زيادة رصيده ولا رصيد غيره أصلاً. والقواعد:

| المحاولة | ما يمنعها |
|---|---|
| يدعو نفسه | `refOk()`: `referredBy != request.auth.uid` |
| يبدّل داعيَه قبل كل طلب | `refOk()`: يُكتب مرّة ولا يُبدَّل |
| يكافأ مرّتين | `refPaid` يُختم مع الرصيد في المعاملة |
| يكتب عدّاده بيده | `adminFieldsKept('refCount' \| 'refEarned' \| 'refPaid')` |
| يدفع بالنقاط ليولّد نقاطاً | الشرط `total > 0` — مالٌ حقيقي أو لا جائزة |
| مئة حساب وهميّ | رقمٌ واحد لحسابٍ واحد (`phones`) + طلبٌ مدفوع |
| يدعو بلا حدّ | `refCap` في إعدادات النقاط |

الإعدادات في تبويب **Points** (`refOn` · `refInviter` · `refInvitee` ·
`refCap`)، والمتابعة في تبويب **Invites**: من سجّل · من اشترى · التكلفة
بالدولار · أفضل الداعين. وحقل الرمز في `/login` بعد الرقم — **بعد حفظه
لا قبله**، لأن القواعد تشترط رقماً صحيحاً في الوثيقة.

✅ **القواعد مُختبَرة بمحرّك Firestore نفسه** (٢٢ حالة على المحاكي): كل
سطرٍ في الجدول أعلاه جُرّب فِعلاً — المنعُ مُنع والسماحُ سُمح. ولإعادة
الاختبار في جلسةٍ قادمة: `firebase emulators:start --only firestore`
مع `@firebase/rules-unit-testing` — لا يلزم مشروعٌ حقيقيّ ولا اتصال.

## ⏳ الخطوة التالية مباشرة

**١) ~~إعادة نشر `firestore.rules`~~ ✅ نشرتها بنفسها (٠١-٠٨)** — مجموعة
`chats` صارت فعّالة والدردشة تُرسل.
**٢) قراءة الـuid** من Firebase ← Authentication ← Users، وإنشاء `admins/{uid}`
في Firestore لتصير أدمن. **٣) لوحة الإدارة** (المرحلة ٤) — وفيها شاشة الردّ
على الدردشة. حتى تُبنى، تُقرأ الرسائل ويُردّ عليها من Firebase Console:
`chats/{uid}/messages` ← رسالة جديدة بحقول `from: "admin"` و`text` و`createdAt`.

### 💬 الدردشة المباشرة

`lib/chat.ts` + `components/LiveChat.tsx` — زرّ عائم على **يمين** كل صفحة فوق
القائمة السفلية. لكل زبون محادثة واحدة باسم الـuid، والرسائل لحظية
(`onSnapshot`). القواعد تمنع الزبون من كتابة رسالة بتوقيع `admin`، فلا ينتحل
أحد صفة المتجر. الزائر يرى دعوة للدخول بضغطة، وبلا Firebase تظهر القنوات
البديلة — فلا ينكسر شيء في أي حالة.

### ❓ الأسئلة الشائعة — تُعدَّل من اللوحة

`lib/faq.ts` — أربعة أسئلة بأجوبتها. المصدر `settings/faq` في Firestore
**يعلو** نصوص `messages/*.json` ← `chat.faq.<key>.{q,a}`، فبلا حفظٍ تظهر
النصوص الأصلية ولا يفرغ شيء.
الزبون يضغط السؤال فيظهر الجواب **فوراً** بلا انتظار وبلا إرسال رسالة.
تبويب **Q&A** في اللوحة يعدّل السؤال والجواب بثلاث لغات، والمكتوب يظهر
في الدردشة **وفي صفحة المساعدة** معاً — مكانٌ واحد للتعديل.
(صفحة المساعدة `revalidate = 60`، فالتعديل يظهر خلال دقيقة.)

⚠️ لا مساعد آلي في الدردشة — قرار صاحبة المتجر. لا مفتاح ولا كلفة
على كل سؤال، والجواب مكتوب بيدها.

### 💬 شاشة الدردشة في اللوحة — تبويب **Live chat**

`lib/adminChat.ts` + `components/admin/ChatsEditor.tsx`. كانت الردود
تُكتب من Firebase Console يدوياً، وهذا لا يصلح لمتجرٍ يعمل.

**بشكل واتساب** (طلبها): حرفٌ دائريّ بلونٍ ثابت لكل زبون · اسمٌ وآخرُ
سطرٍ ووقتُه · نقطةٌ خضراء لمن ينتظر ردّاً · بحثٌ في الأعلى. وداخل
المحادثة: فقاعاتٌ لها ذيول على أرضٍ مُنقّطة، والوقت في زاوية الفقاعة،
وفاصلُ يومٍ في الوسط، وحقلٌ دائريّ وزرٌّ دائريّ للإرسال.

⚠️ **الذيل لأوّل فقاعة في المجموعة وحدها** — ذيلٌ لكل رسالة يصنع
أسناناً على حافة الشاشة تبدو عطلاً. وأصنافه في `globals.css`
(`.chat-ground` · `.bub` · `.bub-tail` · `.chat-day`).

⚠️ **والمحادثة تملأ الشاشة** (`fixed inset-0 z-50`): تختفي القائمة
وترويسة اللوحة وشريط التبويبات معاً، ويُقفل تمرير الصفحة خلفها.
محادثةٌ داخل إطارٍ فيه تبويبات لا تُشبه واتساب مهما شابهتها فقاعاتها.

**الترتيب هو الميزة**: من ضغط «أريد التحدّث مع شخص» فوق الجميع، ثم من
آخر سطرٍ فيه من الزبون — أي من ينتظر ردّك — ثم المردود عليها.
والرسائل لحظية (`onSnapshot`) داخل المحادثة المفتوحة وحدها.

🔒 **باب `chat` مستقلّ في صلاحيات المساعدين**: من يشحن الطلبات ليس
بالضرورة من يتكلّم باسم المتجر. والقواعد تفرضه: `canDo('chat')` شرطٌ
لسرد المحادثات وقراءتها وكتابة رسالةٍ موقّعة `admin`.
⚠️ وفي `ownerOrAdmin()` يُفحص **صاحب المحادثة أوّلاً** — شرطٌ بلا قراءة
وثائق، فرسائل الزبون العادي لا تكلّف قراءتين لكل رسالة.

✅ ١٦ حالة مُختبَرة على المحاكي، منها: مساعد الشحن لا يسرد ولا يردّ ·
الزبون لا ينتحل صفة المتجر · بوب لا يقرأ محادثة أليس.

### 🔐 لماذا عاد التمرير — لا تُلغِه ثانية

سفاري (آيفون/آيباد) يحجب تخزين المواقع الخارجية، فتسجيل الدخول عبر
`ramaa-store.firebaseapp.com` ينجح عند جوجل ثم **يعود الزبون زائراً**.
الحلّ الموثّق من Firebase هو تمرير `/__/auth/*` على نطاقنا — وهو ما في
`next.config.ts` (`beforeFiles`). وشرطه أن يستثني الوسيط `__` في `proxy.ts`،
وإلا أضاف بادئة اللغة فصار ٤٠٤. الفائدة الثانية: `eramaan.com` هو ما يظهر للزبون.

`lib/firebase.ts` ← `PROXIED_HOSTS`: نطاقنا يُستعمل نطاقَ مصادقة متى كان
المضيف من القائمة، وإلا رجع لنطاق Firebase فلا تنكسر معاينات Vercel المؤقّتة.

---

### (سابقاً)

**١) إضافة نطاق Vercel في Authorized domains** (بدونه لا يفتح دخول جوجل).
**٢) تسجيل دخولها مرة، ثم إنشاء `admins/{uid}`** لتصير أدمن.
**٣) لوحة الإدارة (المرحلة ٤)** — ترفع منها المنتجات والباقات والصور.

---

## 🎨 مقترح تصميم بانتظار قرارها

**"سوق الليل"** — أرض نيلية · ذهب زعفران · شرارة مرجانية.
بطاقات الباقات **قسائم مثقوبة** · الأقسام في **سداسيات** · الأرقام بخط آلة كاتبة.
٣ لوحات: `Night market` (المرشَّحة) · `Coast` · `Salt` الفاتحة. عربي/إنجليزي وليلي/نهاري.

المعاينة: https://claude.ai/code/artifact/814a2b4d-f117-44d0-9d65-fff1f929d1fe
المصدر: `scratchpad/design/ramaan.html` (خارج المستودع — لم يُطبَّق على الموقع)

**اعتمدت المعاينة بالوضع الفاتح حرفياً**، وطُبِّقت على الموقع.
⚠️ الفاتح افتراضي عمداً — لا نتبع إعداد الجهاز، لئلا يرى الزائر غير ما وافقت عليه.
للعودة لاتّباع الجهاز: أعِد كتلة `@media (prefers-color-scheme: dark)` في `app/globals.css`.

---

## 📋 معلّق — بانتظارها

| # | المطلوب منها |
|:--:|---|
| ٠ | **Authorized domains**: تأكيد أن الأربعة موجودة — `ramaan-store.vercel.app` · `eramaan.com` · `www.eramaan.com` · `admin.eramaan.com` |
| ٠ب | **إعادة نشر القواعد** بعد إضافة مجموعة `users` |
| ١ | إضافة `PUBG_ID_API` في Vercel ← Settings ← Environment Variables |
| ٢ | رقم واتساب + إيميل (قالت: من لوحة الإدارة) |
| ٣ | صور المنتجات الحقيقية (`public/images/`) |
| ٤ | الأسعار الحقيقية (الحالية تجريبية) |
| ٥ | موقع Vercel لا يفتح من جهازها رغم `Ready` |

---

## 🔜 المراحل القادمة

- **٣ — Firebase:** وثيقة لكل طلب · فصل بيانات الحسابات الحسّاسة · صور حقيقية · `firestore.rules` صارمة
- **٤ — لوحة الإدارة:** واجهة تعدّل حقول `lib/content.ts`

---

## 🗺️ أين كل شيء

| أريد أن أعدّل… | الملف |
|---|---|
| أي نص أو رقم يراه الزبون | `lib/content.ts` |
| الترجمات | `messages/{en,ar,so}.json` |
| المنتجات والباقات (وحالتها وترتيبها) | `lib/data.ts` |
| الألوان والخط | `app/globals.css` |
| الأيقونات | `components/icons.tsx` |
| قواعد أمان قاعدة البيانات | `firestore.rules` · `storage.rules` |
| الدخول والطلبات | `lib/auth.tsx` · `lib/orders.ts` |

---

## ⚖️ قرارات محسومة — لا تُناقَش ثانية

1. Next.js 16 + Vercel — **لا HTML ولا Netlify**
2. الإنجليزية افتراضية بلا بادئة · `ar` (RTL) · `so`
3. Firebase: دخول **بجوجل** فقط (قرارها) · كل طلب وثيقة مستقلّة
4. الرئيسية = إلكترونيات · الألعاب = UC + Coins · الحسابات = تيك توك + eFootball
5. تيك توك = **بيع حسابات**، لا متابعين
6. متجر عالمي — دفع دولي + محلي
7. أسماء الألوان القديمة محفوظة عمداً (`orange`=أزرق، `yellow`=أخضر)
